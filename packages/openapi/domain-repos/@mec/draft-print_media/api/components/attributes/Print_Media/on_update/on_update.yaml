message:
  parent: true
  order:
    parent: true
    provider:
      parent: true
      locations:
        description: locations as listed
        required: mandatory only if in /confirm
        usage: "-"
        reference: if any
    fulfillments:
      parent: true
      fulfillments:
        description: contains the fulfillment details
        required: required
        usage: "-"
        reference: if any  
      stops:
        type:
          description: Type of the fulfillment; (start or end)
          required: mandatory
          usage: "start"
          reference: if any
        instructions:
          name:
            description: name of the instruction
            required: optional
            usage: "special instructions"
            reference: if any
          short_desc:
            description: description of the instruction
            required: optional
            usage: "customer special instructions"
            reference: if any
      person:
        name:
          description: name of the person providing the service
          required: optional
          usage: "Ramu"
          reference: if any
      contact:
        phone:
          description: phone number of the person providing the service
          required: optional
          usage: "9886098860"
          reference: if any


